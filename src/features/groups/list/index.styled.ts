import styled from 'styled-components';
import { PageContentWrapper, Row } from '../../../components/Containers';

export const ContentWrapper = styled(PageContentWrapper)`
  padding-top: 40px;
  flex: 1;
  gap: 30px;
`;

export const FilterTabsWrapper = styled(Row)`
  padding: 0 1rem;
`;
